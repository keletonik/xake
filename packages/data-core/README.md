# @xake/data-core

Provider abstraction and canonical data models. Defines `MarketDataProvider`, `ExecutionVenue`, `PortfolioSource` interfaces, plus normalised `Quote`, `Trade`, `Candle`, `OrderBookLevel`, and `NewsItem` types.

Stage 4 delivers the interfaces, the mock provider, and the stamping contract (`source`, `feedClass`, `ageMs`).
