# @xake/ui

XAKE design system: tokens, primitives, and composed components.

Stage 2 delivers tokens (colour, type, spacing, radii, shadow), theme system (dark, darker, light/system), and the Radix-based primitives (Button, Input, Panel, Tabs, Tooltip, Toast, Dialog).
