# infra/db

Database migrations, seeds, and row-level-security policies for Postgres.

Stage 3 introduces the workspace and account schema. Stage 4 adds Timescale-style hypertables for candles. Stage 6 adds the paper trading ledger (orders, fills, positions, balances). Stage 10 applies RLS for workspace isolation.
