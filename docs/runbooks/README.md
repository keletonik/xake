# docs/runbooks

Incident response and on-call runbooks.

Planned: stream reconnect, stale-feed escalation, failed broker auth, rejected orders, duplicate alerts, portfolio drift reconciliation, AI rate-limit degradation, secrets rotation.
